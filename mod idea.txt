creat addon:
gas refining
	from air
	from water splitting

cryocooler:
	turn gas into liquid

bedrock scraper:
	pulls bedrock powder from bedrock

wormhole generator:
	liquid helium to cool it
	bedrock powder for fuel

	trains can go through
		can build off nether portal functionality
	mirrors nbt data of block on other side of portal
		for redstone, pipes, power(gears, shafts, wires)

	speed limits how big the wormhole can be. the faster, the bigger, the more coolent and bedrock dust it takes

gas engine:
	runs on hydrogen


liquid nitrogen for recipes:



TODO List:

wormhole generator:
    ClientPayloadHandler will receive the two wormhole POSs, the two facing directions, and the size of the wormhole.
    It will generate a dict where the keys are the chunkPos of the blocks, and the values are lists of arrays, where the array has:
     - the pos of the linked block
     - the pos of it's partner
    the function will then iterate through the key value pairs:
        It will load the chunk of the key
        It will iterate through the list of arrays:
            It will place a linked block at the pos stored in array[0]
            It will inject the partner pos stored in array[1] into the block entity
        It will close the chunk

// Assuming all positions are in the same general chunk area
    BlockPos firstPos = targetPositions.get(0);
    ChunkPos chunkPos = new ChunkPos(firstPos);
    String ticketId = "my_multiblock_edit";

    try {
        // 1. Force load the chunk once!
        ForcedChunkManager.forceChunk(level, "yourmod", ticketId, chunkPos.x, chunkPos.z, true, true);

        // Allow a quick safety check to ensure it loaded
        if (level.isLoaded(firstPos)) {

            // 2. Loop through and set all blocks efficiently
            for (BlockPos pos : targetPositions) {
                // Example: level.setBlock(pos, Blocks.AIR.defaultBlockState(), 3);
            }

        }

    } finally {
        // 3. Close the chunk exactly once when the entire loop finishes!
        ForcedChunkManager.forceChunk(level, "yourmod", ticketId, chunkPos.x, chunkPos.z, false, false);
    }


DO LATER List:
Immersive Portals inject:       - do NOT waste time pursuing this until IPs is stable in 1.21.X NeoForge.
    2 main options:
        - start using code to make portals, adding only necessary files into my mod, fixing them as I go.
        - try to add the whole mod as a messed up sudo jar-in-jar/api abomination, as no API exists.
    radical option 3:
        update to 1.21.x while porting to NeoForge, them learn Gradle and properly api




Known bugs:
look into why the AtmoExtr instantly gains fluid when network is connected to it
if you setblock a single wormhole generator block (other than a core) and break it, masterpos will return null
    Fix: handle the null so it skips removeAll().

